// Create table for answer cards in database.

// Seed in data for answer cards.

// Create an answer cards model that contains a function to get all answer cards.

// Create function in model to get one card by card id.

// Create function in model to insert drawn card into player answer cards.

// Create function in model to select a random number and check against player answer cards 

// and round answer cards to ensure card id hasn't been drawn already. 
// If so, select new random number. If not, call function to get card by id 
// and add to player answer cards.